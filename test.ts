// tests go here; this will not be compiled when this package is used as an extension.
let image2 = image.create(1000, 1000)
tiles.setCurrentTilemap(tilemap`level2`)
tilemapToImg.GenerateImage(image2)
